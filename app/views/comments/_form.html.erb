<%= bootstrap_form_for(@comment) do |f| %>
  <%= f.text_field :title , label: 'Asunto' %>
  <%= f.text_area :body, label: 'Comentario' %>




  <%= f.select :user,  User.pluck(:nombre,:id), label: 'Usuario'  %>
  <%= f.select :product, Product.pluck(:name, :id) %>
  <%= f.submit "Guardar" %>
<% end %>
