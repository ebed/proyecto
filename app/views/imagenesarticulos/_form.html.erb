
<%= bootstrap_form_for(@imagen) do |fim| %>
    <%= fim.select :product_id, Product.all.collect {|p| [ p.name, p.id ] } %>

    <%= fim.file_field :image %>



  <%= fim.submit "Guardar" %>
<% end %>

