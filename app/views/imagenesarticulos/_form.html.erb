
<%= bootstrap_form_for(@imagen) do |f| %>
    <%= f.select :product_id, Product.all.collect {|p| [ p.name, p.id ] } %>
    <%= f.text_field :order %>
    <%= f.file_field :file %>



  <%= f.submit "Guardar" %>
<% end %>

