<% @imagenes.each do |imagen| %>

<%= link_to imagen.filename, imagenesarticulo_path(imagen) %>

  <%= image_tag imagen.image.thumb('400x200#').url if imagen.image_stored? %>

 <%= link_to 'x', imagenesarticulo_path(imagen), :method => :delete     %>
<br/>
<% end %>
<p>

<%= link_to "Nueva Imagen", new_imagenesarticulo_path %>
