<ul>
  <% @categories.each do |category| %>
    <li>
      <%= category.name %> <%= link_to "X", category_path(category), :method =>:delete %>
    </li>
    <ul>
    <% category.hijos.each do |hijo| %>
      <li> <%= hijo.name %>
    <% end %>
    </ul>
  <% end %>
</ul>
<%= link_to "Nueva Categoria", new_category_path %>
