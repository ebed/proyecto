

 <%= nav_bar fixed: :top, brand: "Proyecto" do %>


      <p class="navbar-text pull-right">
      <% if user_signed_in? %>

        <ul class="nav navbar-nav navbar-right">

          <li>
             <p class="navbar-text">Has entrado como <strong><%= current_user.email %></strong>.</p>
          </li>

          <li>
            <%= render 'layouts/menus/usermenu' %>
           </li>
           <li>
            <%= render 'layouts/menus/sellmenu' %>
           </li>
           <li>
            <%= render 'layouts/menus/adminmenu' %>


           </li>
             <li>
             <%= button_to "Logout", destroy_user_session_path, method: :delete, :class => 'btn btn-danger navbar-btn'  %>
            </li>

        </ul>

      <% else %>
        <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
        <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
      <% end %>

    <% end %>

